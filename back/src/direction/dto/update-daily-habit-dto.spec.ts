import { UpdateDailyHabitDto } from './update-daily-habit-dto';

describe('UpdateDailyHabitDto', () => {
  it('should be defined', () => {
    expect(new UpdateDailyHabitDto()).toBeDefined();
  });
});
